---

---

<header class="kaisei-opti-regular position">
  <div class="mx-auto flex flex-wrap p-2 flex-row items-center">
    <a class="flex flex-row items-center text-gray-900 mb-0">
      <!--
      <a href="/" class="">
        <div class="flex items-center gap-3 p-2 header_bcolor">
          <img src='/images/IMG_4067.PNG' alt="" class="w-10 h-10" />
          <div class="flex flex-col">
            <span class="text-lg font-bold header_tcolor">第8回</span>
            <span class="text-xl font-bold header_tcolor">うぃーすた全国交流会</span>
          </div>
        </div>
      </a>
      -->
      <a class="card-header resp-pc-f back-blur" style="margin-top: 0.2rem; margin-left: 2rem;" href="/">
        <div style="display: flex; align-items: center;">
            <img src='/images/IMG_4067.PNG' alt="" class="w-10 h-10" style="margin-left: 0.7rem;"/>
            <div class="card-box flex flex-col leading-none">
              <span class="text-lg font-bold text-start">第8回</span>
              <span class="text-lg font-bold text-start">うぃーすた全国交流会</span>
            </div>
        </div>
      </a>
      <a class="card-header resp-pc-g back-blur" style="margin-top: 0.75rem; margin-left: 0.75rem;" href="/">
        <div style="display: flex; align-items: center;">
            <img src='/images/IMG_4067.PNG' alt="" class="w-10 h-10" style="margin-left: 0.7rem;"/>
            <div class="card-box flex flex-col">
              <span class="text-sm font-bold text-start">第8回</span>
              <span class="text-sm font-bold text-start">うぃーすた全国交流会</span>
            </div>
        </div>
      </a>
    </a>

    <script>
      const burger = document.querySelector("#burger");
      const close = document.querySelector("#close");
      const menu = document.querySelector("#menu");

      if (burger) {
        burger.addEventListener("click", () => {
          burger?.classList.toggle("is-active");
          close?.classList.toggle("is-active");
          menu?.classList.toggle("is-active");
        });
      }

      if (close) {
        close.addEventListener("click", () => {
          burger?.classList.remove("is-active");
          close?.classList.remove("is-active");
          menu?.classList.remove("is-active");
        });
      }

      // サブメニュー内のリンククリックでメニューを閉じる
const dropdownLinks = document.querySelectorAll(".dropdown-content a");
dropdownLinks.forEach(link => {
  link.addEventListener("click", () => {
    burger?.classList.remove("is-active");
    close?.classList.remove("is-active");
    menu?.classList.remove("is-active");
  });
});

      // 全てのサブメニューのリンクにイベントを追加
      document.querySelectorAll('.dropdown-content a').forEach(link => {
        link.addEventListener('click', () => {
          const dropdown = link.closest('.dropdown') as HTMLElement | null;
          if (!dropdown) return;

          const content = dropdown.querySelector('.dropdown-content') as HTMLElement | null;
          if (!content) return;

          content.style.display = 'none'; // OK
        });
      });


        // 親のホバーで再表示
        document.querySelectorAll('.dropdown').forEach(dropdown => {
        const dropdownEl = dropdown as HTMLElement; // 型を明示
        dropdownEl.addEventListener('mouseenter', () => {
          const content = dropdownEl.querySelector('.dropdown-content') as HTMLElement | null;
          if (!content) return;
          content.style.display = 'block';
        });

        dropdownEl.addEventListener('mouseleave', () => {
          const content = dropdownEl.querySelector('.dropdown-content') as HTMLElement | null;
          if (!content) return;
          content.style.display = 'none';
        });
      });

      document.addEventListener("DOMContentLoaded", () => {
  const dropdown = document.querySelector(".dropdown");
  const dropdownContent = dropdown?.querySelector(".dropdown-content");

  // dropdownがない場合は何もしない
  if (!dropdown || !dropdownContent) return;

  // ▼ 親メニュークリックでサブメニュー開閉
  dropdown.addEventListener("click", (e) => {
    e.stopPropagation();
    dropdownContent.classList.toggle("is-active");
  });

  // ▼ 画面クリックでサブメニューを閉じる
  document.addEventListener("click", (e) => {
  if (!dropdown.contains(e.target as Node)) {
    dropdownContent.classList.remove("is-active");
  }
});
});




      document.addEventListener("DOMContentLoaded", () => {
  const topbars = document.querySelectorAll(".topbar");
  const hero = document.querySelector(".img-frame.hero");
  const isHome = window.location.pathname === "/" || window.location.pathname === "/index.html";

  if (!isHome) {
    // トップページ以外 → 常に表示
    topbars.forEach(el => el.classList.add("visible"));
    return;
  }

  if (!hero) {
    // ヒーロー画像がない場合も常時表示
    topbars.forEach(el => el.classList.add("visible"));
    return;
  }

  // トップページ：スクロールで表示切替
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.intersectionRatio > 0) {
        // ヒーローが画面内にある → 非表示
        topbars.forEach(el => el.classList.remove("visible"));
      } else {
        // ヒーローが画面外 → 表示
        topbars.forEach(el => el.classList.add("visible"));
      }
    });
  }, { threshold: [0, 0.01] });

  observer.observe(hero);
});




    </script>

    <nav class="ml-auto flex flex-wrap items-center text-base justify-center max-xl:mr-2.5 xl:mr-8" style="margin-top: 0.5rem;">
      <div class="menu_header header_tcolor header_container">
        <div class="card-header pic-image dropdown">
          <a class="back-blur" href="/about/">
            <div class="card-box">
              <p class="card-title" style="padding: 0.15rem;">うぃーすたについて</p>
            </div>
          </a>
          <div class="dropdown-content">
            <a href="/about/#stutter">吃音とは</a>
            <a href="/about/#westu">うぃーすたプロジェクトとは</a>
            <a href="/about/#westu_koryu">うぃーすた全国交流会とは</a>
          </div>
        </div>
        
        <a class="card-header back-blur" href="/posts/1">
          <div class="pic-image">
              <div class="card-box">
                  <p class="card-title" style="padding: 0.15rem;">お知らせ</p>
              </div>
          </div>
        </a>
        <a class="card-header back-blur" href="/programs/">
          <div class="pic-image">
              <div class="card-box">
                  <p class="card-title" style="padding: 0.15rem;">日程・プログラム</p>
              </div>
          </div>
        </a>
        <a class="card-header back-blur" href="/attentions/">
          <div class="pic-image">
              <div class="card-box">
                  <p class="card-title" style="padding: 0.15rem;">注意事項</p>
              </div>
          </div>
        </a>
        <a class="card-header back-blur" href="/register/">
          <div class="pic-image">
              <div class="card-box">
                  <p class="card-title" style="padding: 0.15rem;">お申し込み</p>
              </div>
          </div>
        </a> 
        <a class="card-header back-blur" href="/contact/">
          <div class="pic-image">
              <div class="card-box">
                  <p class="card-title" style="padding: 0.15rem;">お問い合わせ</p>
              </div>
          </div>
        </a>
      </div>
      <a class="resp-pc-g">
        <div class="pic-image flex items-center justify-center">
            <div class="card-box">
              <button id="burger" class="burger">
                <span></span>
                <span></span>
                <span></span>
              </button>
            </div>
        </div>
      </a>
    </nav>
  </div>
  <nav class="menu" id="menu">
    <button id="close" class="burger is-active"></button>
    <div class="card-header pic-image dropdown" style="padding-top: 120px;">
      <a class="back-blur" href="/about/">
        <div class="card-box">
          <p class="card-title" style="padding: 0.5rem;">うぃーすたについて</p>
        </div>
      </a>
      <div class="dropdown-content">
        <a href="/about/#stutter">吃音とは</a>
        <a href="/about/#westu">うぃーすたプロジェクトとは</a>
        <a href="/about/#westu_koryu">うぃーすた全国交流会とは</a>
      </div>
    </div>
    <a class="card-header back-blur" href="/posts/1">
      <div class="pic-image">
          <div class="card-box">
              <p class="card-title" style="padding: 0.5rem;">お知らせ</p>
          </div>
      </div>
    </a>
    <a class="card-header back-blur" href="/programs/">
      <div class="pic-image">
          <div class="card-box">
              <p class="card-title" style="padding: 0.5rem;">日程・プログラム</p>
          </div>
      </div>
    </a>
    <a class="card-header back-blur" href="/attentions/">
      <div class="pic-image">
          <div class="card-box">
              <p class="card-title" style="padding: 0.5rem;">注意事項</p>
          </div>
      </div>
    </a>
    <a class="card-header back-blur" href="/register/">
      <div class="pic-image">
          <div class="card-box">
              <p class="card-title" style="padding: 0.5rem;">お申し込み</p>
          </div>
      </div>
    </a> 
    <a class="card-header back-blur" href="/contact/">
      <div class="pic-image">
          <div class="card-box">
              <p class="card-title" style="padding: 0.5rem;">お問い合わせ</p>
          </div>
      </div>
    </a>
  </div>
    <div style="height: 10px;"></div>
  </nav>
</header>
